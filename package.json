{
  "name": "simetriapp",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@simetriapp/web",
    "dev:api": "npm run dev --workspace=@simetriapp/api",
    "dev:full": "npm run dev --workspace=@simetriapp/api & npm run dev --workspace=@simetriapp/web",
    "build": "npm run build --workspace=@simetriapp/shared && npm run build --workspace=@simetriapp/web",
    "db:migrate": "npm run db:migrate --workspace=@simetriapp/api",
    "db:seed": "npm run db:seed --workspace=@simetriapp/api"
  },
  "engines": {
    "node": ">=20"
  }
}
